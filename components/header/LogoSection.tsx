// material-ui
import { ButtonBase } from "@mui/material";
import Logo from "../common/Logo";

// project imports

// ==============================|| MAIN LOGO ||============================== //

const LogoSection = () => {
  return (
    <ButtonBase>
      <Logo />
    </ButtonBase>
  );
};

export default LogoSection;
