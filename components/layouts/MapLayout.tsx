import { Box } from "@mui/material";
import { ReactElement } from "react";

const MapLayout = (page: ReactElement) => {
  return (
    <>
      <Box>Map layout</Box>
      {page}
    </>
  );
};

export default MapLayout;
